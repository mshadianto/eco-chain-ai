import React from "react";
import ReactDOM from "react-dom/client";
import EcoChain from "../ecochain-marketplace.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EcoChain />
  </React.StrictMode>
);
